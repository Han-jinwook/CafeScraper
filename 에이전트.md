# 🤖 에이전트 인수인계 지침서 (에이전트.md)

- **문서 버전**: v2.0 (2026-09-18 기준)
- **적용 대상**: 새 세션에 진입하는 모든 AI 어시스턴트 / 에이전트
- **사용자 호칭**: 반드시 **"멀린님"**으로 호칭할 것.
- **마스터 문서**: `d:\3Monster\에이전트.md` 참조

---

## 📌 1. 프로젝트 개요 및 워크스페이스 구조

| 워크스페이스 경로 | GitHub 원격 저장소 | 프로젝트 역할 |
| :--- | :--- | :--- |
| `d:\3Monster` | `sundream7878/3Monster` | 관리자 대시보드 (`admin-dashboard`), 자사몰 웹, 허브 문서 및 영업자료 |
| `d:\CafeScraper` | `Han-jinwook/CafeScraper` | 네이버 카페 크롤러 (`CafeCrawler`), 댓글 이벤트 추첨기 (`EventStats`), 자동 댓글기 (`AutoComment`) |
| `d:\N-Place-DB` | `sundream7878/n-place-db` | 네이버 플레이스 타겟 DB 수집기 (`NPlace-DB`) |

* **호스팅/배포 플랫폼**: 자사몰 및 대시보드는 **넷리파이(Netlify)** 기반 (절대 Vercel로 착각하지 말 것).
* **데이터베이스**: Supabase Cloud PostgreSQL (`licenses`, `users`, `support_tickets`, `app_versions` 등)

---

## ⚡ 2. 최신 비즈니스 룰 및 운영 원칙 (필수 숙지)

1. **고객 식별 단일화 (이메일 단일 인증)**:
   * 크몽 ID, 닉네임, 별도 회원 ID 체계는 완전히 폐지되었습니다.
   * 모든 고객 식별, 라이선스 매칭의 유일한 기준은 **`고객 이메일`**입니다.

2. **라이선스 상태 3단계 체계 (`대기중` 완전 삭제)**:
   * 과거의 `unused`(`대기중`) 상태는 실사용(first_run_date) 기준 삭제와 함께 전면 폐지되었습니다.
   * 모든 유료 라이선스는 **구매일(created_at)로부터 30일 이용권**으로 가동되며, 상태는 **`정상` / `만료` / `정지`** 3단계로만 관리합니다.

3. **실시간 수집량 동기화 (`used_count`)**:
   * Supabase `licenses` 테이블에 **`used_count`** 컬럼이 운영 중입니다.
   * `CafeScraper`의 카페 크롤러, 이벤트 추첨기, 자동 댓글기 등은 한도 플랜뿐 아니라 **무제한 플랜(PRO/DELUXE)에서도 데이터 수집 시 Supabase `licenses.used_count`에 실시간 자동 PATCH** 동기화합니다.

4. **클라이언트 UI 작업 리셋 표준**:
   * 클라이언트 앱의 초기화 버튼은 데이터를 삭제하는 것이 아니라 **`🆕 새 작업 시작 (화면 비우기)`**로 동작합니다.
   * 사용자가 수집한 기존 DB/엑셀 파일은 작업 폴더에 안전하게 보관되고, 화면 통계만 새 작업을 위해 비워집니다.

5. **재구독 및 업그레이드 체계**:
   * 정기 카드 자동결제, 일할 차액 계산 결제, 충전 부스터 제도는 운영하지 않습니다.
   * 한도 소진 또는 만료 시 자사몰에서 30일 이용권을 신규 재결제하는 방식입니다.

6. **관리자 대시보드 테이블 UI 규격**:
   * 메인 라이선스 목록의 우측 제어 컬럼은 **`[정지/해제]`(Power)** 및 **`[삭제]`(Trash2)** 아이콘만 남겨 **컬럼 폭을 80px로 슬림화**했습니다.

---

## 🔱 3. 에이전트 절대 행동 강령 (멀린강령 요약)

1. 사용자를 부를 때는 언제나 **"멀린님"**으로 호칭한다.
2. 멀린님의 명시적인 허락/승인 없이 소스코드를 임의로 변경하거나 앞서나가지 않는다.
3. 맹목적 동조를 지양하고, 냉정하고 객관적인 엔지니어링 피드백을 제시한다.
4. 임시방편(땜빵) 패치를 금지하고, 무조건 근본적인 구조 개선 및 원인 치료를 원칙으로 한다.
5. 기능 변경 시 관련 명세서(`docs/`) 및 강령 문서를 함께 최신 상태로 갱신한다.
