# 네이버 카페 크롤러 - 로컬 GUI 실행 가이드

## 🚀 빠른 시작

### 1. 패키지 설치
```powershell
# 가상환경 활성화 (이미 있다면)
. .\.venv\Scripts\Activate.ps1

# 패키지 설치
pip install -r requirements.txt
```

### 2. Streamlit 앱 실행
```powershell
streamlit run 카페_게시글_댓글_크롤러.py
```

브라우저가 자동으로 열리고 크롤러 GUI가 표시됩니다.

## 📋 사용 방법

### 크롬 프로필 경로 설정
1. **자동 감지**: Windows 기본 경로가 자동으로 감지됩니다
2. **수동 입력**: 자동 감지가 안되면 다음 경로를 입력하세요
   ```
   C:\Users\사용자명\AppData\Local\Google\Chrome\User Data
   ```

### 크롤링 모드 선택

#### 1. 게시판 수집
- 특정 게시판의 모든 게시글을 수집합니다
- **게시판 URL** 입력 필요
- 예: `https://cafe.naver.com/yourcafe/BoardList.nhn?clubid=...&menuid=...`

#### 2. 작성자별 수집
- 특정 작성자의 모든 게시글을 수집합니다
- **작성자 닉네임** 입력 필요
- 여러 게시판을 순회하며 해당 작성자의 게시글을 찾습니다

#### 3. 키워드 검색
- 카페 내에서 키워드로 검색하여 게시글을 수집합니다
- **검색 키워드** 입력 필요
- 검색 결과 페이지에서 게시글을 수집합니다

### 공통 설정
- **카페 URL**: 크롤링할 네이버 카페 메인 URL
- **게시판 URL**: 전체글보기 권장 (예: `https://cafe.naver.com/f-e/cafes/.../menus/0`)
- **수집 기간**: 시작일 ~ 종료일 범위 내 게시글만 수집
- **종료일 기준 자동 시작페이지** (권장): 체크 시 종료일에 맞는 페이지를 이진 탐색으로 자동 점프. 수동 지정보다 안정적.
- **추천 시작페이지 미리보기**: 브라우저 열린 상태에서 해당 기간의 추천 페이지·날짜 범위를 사전 확인 가능
- **수동 시작 페이지**: 자동 해제 시 이전 실행의 마지막 페이지 근처를 직접 지정 가능

### 댓글 필터링
- **포함할 닉네임**: 이 닉네임들의 댓글만 수집 (쉼표로 구분)
- **제외할 닉네임**: 이 닉네임들의 댓글은 제외 (쉼표로 구분)

## ⚙️ 주요 기능

### ✅ 구현된 기능
- ✅ 2단계 워크플로우 (1단계: 브라우저 열기 → 2단계: 크롤링 시작)
- ✅ 로컬 크롬 프로필 연동 (수동 로그인 후 세션 유지)
- ✅ **종료일 기준 자동 시작페이지**: 이진 탐색으로 해당 기간 페이지 자동 점프 (기본값: 켜짐)
- ✅ 추천 시작페이지 미리보기 (브라우저 열린 상태에서 사전 확인)
- ✅ iframe 전환 자동 처리
- ✅ 50개씩 보기 자동 전환 (탐색 속도 향상)
- ✅ 날짜 필터링 (시작일~종료일)
- ✅ 실시간 진행 요약 (예상 건수, ETA, 완료율)
- ✅ 체크포인트 저장/재개 (중단 후 이어서 수집)

### 🔧 기술 사양
- **브라우저**: Chrome (헤드리스 모드 없음, 눈에 보이게 실행)
- **딜레이**: `random.uniform(3, 10)` 초
- **저장 형식**: CSV (기존 형식 유지)
- **실행 환경**: 로컬만 (서버 배포 안함)

## 📁 출력 파일

수집된 데이터는 SQLite DB에 저장됩니다 (설정에서 경로 변경 가능):
- 기본 경로: 프로젝트 루트 또는 `CAFESCRAPER_DB_PATH` 환경변수
- 로그: `logs/crawler_YYYYMMDD.log`

## ⚠️ 주의사항

1. **로컬 실행만 가능**: 서버 배포 환경에서는 작동하지 않습니다
2. **크롬 프로필**: 크롬 브라우저가 실행 중이면 프로필을 사용할 수 없을 수 있습니다
3. **이용약관**: 네이버 카페 이용약관을 준수하여 사용하세요
4. **과도한 요청**: 적절한 딜레이를 두고 사용하세요

## 🐛 문제 해결

### 크롬 프로필 경로를 찾을 수 없음
- 크롬 브라우저를 완전히 종료한 후 다시 시도
- 수동으로 경로 입력

### iframe 전환 실패
- 페이지 로딩 시간을 늘려보세요
- 네트워크 연결을 확인하세요

### 검색 기능이 작동하지 않음
- 카페 내 검색 UI 구조가 변경되었을 수 있습니다
- 페이지 구조를 확인하고 셀렉터를 업데이트해야 할 수 있습니다

## 📝 개발 노트

- 기존 FastAPI 서버 코드는 유지되어 있습니다
- 새로운 크롤러는 `crawler.py`에 구현되어 있습니다
- Streamlit UI는 `app.py`에 구현되어 있습니다
