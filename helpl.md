# 🆘 Help Needed: 네이버 카페 크롤러 긴급 진단 및 해결 가이드

이 파일은 현재 세션에서 해결하지 못한 기술적 난제들을 정리한 문서입니다. 다음 세션의 AI는 이 내용을 바탕으로 작업을 이어가시기 바랍니다.

## 1. 문제 상황 요약
네이버 카페의 최신 SPA(Single Page Application) 구조와 강력해진 차단 메커니즘으로 인해, 게시판 목록까지는 접근하나 **실제 게시글 데이터를 1건도 추출하지 못하는 현상**이 반복되고 있습니다.

## 2. 핵심 기술적 난제 (Technical Challenges)

### A. 게시글 링크 인식 및 ID 추출 실패
- **증상**: `✅ 후보 링크 52개 발견`과 같이 링크는 찾지만, `✅ 최종 게시글 0개 추출 완료`로 끝남.
- **원인 추정**: 
    1. 네이버의 새로운 URL 패턴(예: `/vpqtnl/12345`)에서 게시글 ID를 정규식으로 뽑아내지 못함.
    2. 게시글 링크의 부모 요소(tr, li 등)를 찾는 과정에서 DOM 구조가 예상과 달라 `author`, `date` 추출에 실패하고 결과적으로 해당 링크를 버림.
- **해결 시도**: 정규식 강화, `data-article-id` 속성 확인, Fallback 로직 추가 등을 수행했으나 여전히 불안정함.

### B. 네이버 "Sorry" 페이지 (차단)
- **증상**: 크롤링 도중 "잠시 후 다시 확인해주세요" 이미지 페이지 노출.
- **특이사항**: 단순히 딜레이를 주는 것만으로는 부족하며, 쿠키를 삭제하거나 브라우저를 재시작해야 하는 경우가 많음.
- **필요 조치**: 더 정교한 Human Mimicry(마우스 이동, 불규칙한 스크롤, 랜덤한 클릭 등)가 필요함.

### C. Selenium 세션 강제 종료
- **증상**: `invalid session id: session deleted as the browser has closed the connection` 에러 발생.
- **원인 추정**: 네이버 측의 강제 연결 끊기 또는 Streamlit 세션 관리와의 충돌.

## 3. 다음 세션 AI를 위한 가이드라인

### 🔍 1순위 분석 대상: `_extract_article_links_from_board`
이 메서드가 현재 가장 취약합니다. 
- 발견된 `found_links`의 `href` 샘플을 로그로 찍어보고, 네이버 카페의 **진짜 게시글 링크** 형식이 무엇인지 재정의해야 합니다.
- 특히 `ancestor::tr`이나 `ancestor::li`를 찾는 로직이 실패할 경우를 대비해, 링크 텍스트 자체에서 정보를 파싱하는 극단적인 Fallback이 필요할 수 있습니다.

### 🛡 2순위 분석 대상: 차단 우회
- `_random_delay`를 단순히 `time.sleep`으로 처리하지 말고, `ActionChains`를 이용해 화면의 빈 공간을 클릭하거나 마우스를 무작위로 움직이는 코드를 추가하세요.
- User-Agent뿐만 아니라 `window.navigator`의 각종 속성들을 더 완벽하게 Stealth 상태로 만들어야 합니다.

### 💾 3순위 분석 대상: DB 저장
- 현재 제공된 DB 스키마(`cafe_posts`, `cafe_comments`)와 `app.py`의 `save_to_supabase` 함수는 매핑이 완료되었습니다. 데이터만 넘어오면 저장은 문제없을 것입니다.

## 4. 마지막 메시지
사용자님은 오늘 하루 종일 이 문제로 고생하셨습니다. 다음 세션에서는 **"왜 링크는 찾는데 데이터는 0개인가"**라는 질문에 대해 DOM 구조를 직접 하나하나 뜯어보는 수준의 정밀한 디버깅부터 시작해 주시기 바랍니다.
