## 로컬 SQLite DB 스키마 (Project DAYBREAK)

- **DB 파일**: `cafe_data.db`
- **특징**: DB 파일 1개 안에 `posts`, `comments`, `papers` 등 여러 테이블을 함께 사용
- **업서트(재개/resume)**:
  - `posts`: `post_id` 기준 `INSERT OR REPLACE`
  - `papers`: `url` 기준 `INSERT OR REPLACE`

---

## 1) posts (카페 게시글)

### 목적
네이버 카페 게시글(본문 포함) 저장.

### 컬럼
- **post_id** `TEXT` **PRIMARY KEY**
  - 게시글 `articleid` (예: `"23288"`)
- **member_id** `TEXT`
  - 작성자 고유키(긴 MemberKey 계열 포함). 닉네임이 바뀌어도 불변 키로 사용
- **nickname** `TEXT`
  - 작성자 표시 닉네임
- **title** `TEXT`
  - 게시글 제목
- **content** `TEXT`
  - 게시글 본문 텍스트(정제 후 저장)
- **date** `TEXT`
  - 작성일 (`YYYY-MM-DD`)
- **board_name** `TEXT`
  - 게시판명(예: “가입인사”, “자유게시판”)
- **url** `TEXT`
  - 게시글 URL
- **created_at** `TIMESTAMP` (DEFAULT `CURRENT_TIMESTAMP`)
  - DB에 최초로 레코드 생성된 시간(자동)

---

## 2) comments (카페 댓글)

### 목적
게시글 댓글 저장(관계 중심 수집 시 필터링된 댓글만 저장 가능).

### 컬럼
- **comment_id** `INTEGER` **PRIMARY KEY AUTOINCREMENT**
  - 내부 자동 증가 키
- **post_id** `TEXT`
  - 원글 ID (`posts.post_id`) 참조
- **writer_id** `TEXT`
  - 댓글 작성자 고유키(긴 MemberKey 계열 포함)
- **nickname** `TEXT`
  - 댓글 작성자 닉네임
- **content** `TEXT`
  - 댓글 텍스트
- **is_target** `INTEGER`
  - 수집 대상 여부(예: 본문 작성자/운영자 댓글만 1로 저장)
- **FOREIGN KEY(post_id) REFERENCES posts(post_id)**

---

## 3) papers (VitaminDWiki 전수조사)

### 목적
VitaminDWiki의 `/pages/*` 페이지를 전수 수집해 로컬 지식 DB 구축.

### 컬럼
- **url** `TEXT` **PRIMARY KEY**
  - 페이지 URL (예: `https://vitamindwiki.com/pages/acne/`)
- **title** `TEXT`
  - 페이지 제목
- **summary** `TEXT`
  - 미리보기용 짧은 텍스트(앞부분만 잘라 저장, AI 요약 아님)
- **content** `TEXT`
  - 페이지 본문 전체 텍스트
- **category** `TEXT`
  - 페이지의 태그/카테고리 문자열 (예: `Skin | Intervention`)
  - 구분자: `" | "` (파이프)
- **collected_date** `TEXT`
  - 수집일 (`YYYY-MM-DD`)
- **created_at** `TIMESTAMP` (DEFAULT `CURRENT_TIMESTAMP`)
  - DB에 최초로 레코드 생성된 시간(자동)

---

## 마이그레이션(구 DB 호환)

- `migrate_db.py`에서 아래 호환 처리를 수행
  - `comments.is_admin` → `comments.is_target`로 이관(또는 `is_target` 추가)
  - `posts.board_name` 컬럼이 없으면 `ALTER TABLE`로 추가
  - `papers` 테이블 생성(없으면)
  - `papers.content` 컬럼이 없으면 `ALTER TABLE`로 추가 + 기존 `summary`를 `content`로 백필

