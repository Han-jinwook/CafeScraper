<!-- CafeScraper 1.3.43 -->

# 변경 이력

| 항목 | 값 |
|------|-----|
| **제목** | 변경 이력 |
| **버전** | 1.3.43 (`version.txt`) |
| **일시** | 2026-06-12 |

**규칙:** 릴리스할 때마다 루트 `version.txt`의 **semver**(한 줄)를 바꾸고, 여기 **맨 위에** 요약 블록을 추가합니다.
- **패치(+끝자리):** 작은 수정, 버그픽스, 문구/UI만, 의미 있는 동작 변경이 거의 없을 때.
- **마이너(+가운뎃자리):** 새 기능·흐름 추가, 동작 방식이 눈에 띄게 바뀌지만 기존 쓰는 사람 입장에서 “큰 깨짐”은 아닐 때.
- **메이저(+맨 앞자리):** 호환 깨지는 변경(설정 형식 폐기, API/파일 규격 불일치 등) 때.

## 1.3.43 — 2026-06-12

- **UI/CSS 및 카드 레이아웃 오버플로우 핫픽스 (UX 폴리싱):** 
  - `app.py`에 이중으로 존재하던 하드코딩된 예전 settings card CSS 코드를 전면 삭제하고, 공통 `inject_settings_three_cards_css`를 호출하도록 정상화했습니다.
  - 설정 카드 3개의 스타일을 딥 네이비 / 블루 포인트 테마 기반의 흰색 배경 및 연한 테두리(`background: #ffffff`, `border: 1px solid #cbd5e1`) 평면형 카드로 단순화했습니다.
  - 카드 타이틀의 불필요한 배경색 및 전방향 테두리를 제거하고 플랫한 블루 밑줄 포인트 텍스트로 수정하여 카드 밖으로 텍스트 및 입력창이 튀어나가는 현상을 완전히 해결했습니다.

## 1.3.41 — 2026-06-09

- **네이버 카페 조회수 부스터 (View Count Booster) 기능 추가 (4번째 메뉴 연동):** 가계정(다중 계정)을 교대로 순회 로그인하여 내가 운영하는 카페의 특정 게시글에 대해 조회수를 안전하게 올리는 기능을 신설했습니다.
  - **보안 제재 방어 및 즉시 중단:** 로그인 중 캡차(CAPTCHA) 감지 또는 2단계 인증 요구 시, 작업을 즉시 안전하게 종료하여 IP/계정 추가 제재를 방지합니다.
  - **선택적 타겟팅 및 필터링:** 전체글/전체공지글/특정게시판 중 타겟 글을 수집한 뒤, 체크박스로 원하는 글만 선택하여 부스팅을 실행할 수 있습니다.
  - **안전 랜덤 딜레이:** 글 1개당 10~60초 대기, 계정 전환 시 1~5분 대기(기본 권장 설정) 등 랜덤 휴식을 적용하여 네이버 Throttling 탐지를 회피합니다.
  - **쿠키 및 세션 완전 격리:** 다중 로그인 순회 시 이전 계정 세션이 오염되지 않도록 이전 계정 로그아웃 처리 및 `driver.delete_all_cookies()`를 엄격하게 적용했습니다.
  - **가계정 관리 및 영구 보관:** `crawler_config.json` 내 `booster_accounts_raw` 키를 통해 입력한 ID,PW 목록이 영구적으로 자동 보관 및 복원됩니다.

## 1.3.40 — 2026-06-09

- **초기 로딩 화면 잔상 개선 (UX 폴리싱):** 앱 최초 실행 시 1~2초 동안 Streamlit 기본 사이드바 네비게이션과 로딩 스켈레톤(Skeleton) 화면이 깜빡이며 나타났다 사라지는 잔상 문제를 완벽히 해결했습니다. Streamlit의 `index.html` 템플릿에 직접 CSS 스타일을 사전에 주입하도록 `build.bat`에 패치 단계를 추가하여, 브라우저가 화면을 로드하는 첫 시작 시점부터 불필요한 UI 흔적이 감춰지도록 함으로써 부드럽고 완성도 높은 UI 첫인상을 제공합니다.

## 1.3.37 ~ 1.3.39 — 2026-06-09

- **결과 폴더 열기 기능 추가 및 라이브러리 연동:** 수집 완료 결과 화면 하단에 데이터베이스(DB) 및 다운로드 폴더를 직접 시스템 탐색기에서 열 수 있는 버튼을 추가하고 관련 라이브러리 의존성을 연동했습니다.
- **이벤트 별명 스킵 로그 개선:** 별명 필터링에 의해 게시글이 생략될 때, 어떤 작성자명 때문에 스킵되었는지 로그 콘솔에 작성자 닉네임을 구체적으로 명시하도록 로깅 가시성을 개선했습니다.
- **네이버 카페 SPA/Legacy URL 호환 및 PC 모드 iframe 포커싱 보강:** 신형 SPA 스타일 URL 구조(`/ca-fe/`, `/f-e/`)와 구형 Legacy URL 구조(`ArticleList.nhn`) 간 상호 호환 파싱 로직을 정교화하고, PC 브라우저 모드에서 SPA 형식 주소 처리 시 `cafe_main` iframe 전환 포커스를 누락하여 빈 목록으로 집계되던 호환성 오류를 수정했습니다.

## 1.3.36 — 2026-06-08

- **크롤링 엔진 개선 (성능 및 안정성 향상):** 카페 페이지네이션 시 매번 전체 브라우저 창(Top-level)을 재배치 및 리다이렉트하여 발생하던 불필요한 네트워크 지연(약 40초 대기 유발)을 해결했습니다. 이미 카페 페이지에 진입한 상태라면 iframe 내부에서 JavaScript(`location.href`)를 사용해 직접 페이지를 전환하도록 변경하여, 리다이렉션으로 인한 로딩 지연 및 iframe 전환 실패(0건 수집 유발) 문제를 근본적으로 해결했습니다.

## 1.3.35 — 2026-06-08

- **UI/UX 개선 (전체 공통):** 네이버 아이디 또는 비밀번호를 입력하고 `Tab` 키를 누르거나 포커스를 잃었을 때 '자동로그인 설정' 접기/펼치기(`st.expander`) 창이 강제로 닫혀버리는 현상을 해결했습니다. 세션 상태(`expanded` 파라미터)와 입력창 `on_change` 이벤트를 바인딩하여 입력 도중에 접히지 않도록 상태를 고정 유지합니다.
- **이벤트 댓글 분석:** 앱을 새로 켰을 때 기존에 저장해둔 카페 설정(카페명, 카페 URL) 및 사이드 연결 모드 상태가 정상적으로 로드되지 않고 화면에서 빈 칸으로 초기화되던 초기 바인딩 버그를 수정했습니다.

## 1.3.34 — 2026-06-08

- **이벤트 댓글 분석:** 조건(3) 등급별 방문수 단독 수집 실행 시, 화면 프리징(멈춤)을 해결하고 실시간 진행 상황(현재 작업 등급, 누적 수집 행수, 경과 시간)과 프로그레스 바가 정상 표시되도록 실시간 모니터링 UI 및 전용 콜백 연동을 추가했습니다.

## 1.3.33 — 2026-06-08

- **자동댓글러:** 2단계 타겟 목록 수집 시 UI 프리징 현상을 방지하기 위해 백그라운드 스레드 및 실시간 진행률 프로그레스 바 UI 연동 방식으로 구조를 전면 개편했습니다.

## 1.3.32 — 2026-06-05

- **메뉴 정리:** 사이트 DB(논문 수집) 기능의 독립 분리로 인해 상단 메뉴 탭을 3단("카페 수집", "이벤트 댓글 분석", "자동 댓글러")으로 간소화하고, 관련 소스코드(`02_paper_collection.py` 등) 및 설정을 프로젝트에서 완전히 정리했습니다.

## 1.3.31 — 2026-06-04

- **자동댓글러:** 2단계 목록 수집 도중 예외가 발생했을 때 조용히 뭉개져서 무한 대기(Hang) 현상이 생기는 버그를 해결하여 즉시 중단되게 하고, 자식 Streamlit 프로세스의 실시간 파일 로깅(`PYTHONUNBUFFERED=1`)을 추가했습니다.

## 1.3.30 — 2026-06-04

- **크롤러:** 자동댓글러 목록 수집 시 `YYYY.MM.DD.` 형식의 날짜 파싱 오류 해결 및 예외 처리 보강 (끝 마침표 제거 대응).

## 1.3.29 — 2026-06-04

- **논문 수집:** 사이트 콘텐츠 수집기 백그라운드 스레드 및 Queue 기반 리팩토링 (`pages/02_paper_collection.py` 리팩토링).

## 1.3.28 — 2026-05-19

- **자동댓글러:** 타겟 표 비었을 때 세션 때문에 **SQLite 재로드를 건너뛰거나**(기존 `target_df is None`만 허용) 빈 표로 **`commenter_target_df_full`** 이 깨지던 경로 완화 — **양쪽 비면 DB 로드**, **풀 스냅샷만 있으면 표 복구**, **풀이 비면 DB로 풀만 채움**. 날짜 **적용** 시 스냅샷 없으면 **빈 `target_df` 대입 차단**.

## 1.3.27 — 2026-05-19

- **자동댓글러 UI:** 타겟 목록 아래 **작성일 적용만**(행 구간·설명글 제거), 한 줄형 컨트롤로 단순화.

## 1.3.26 — 2026-05-19

- **자동댓글러:** `04_auto_commenter.py` 📊 목록 표시 범위 `expander` 블록 **들여쓰기 수정** (`IndentationError`로 페이지 로드 불가 수정).

## 1.3.25 — 2026-05-19

- **자동댓글러 UI:** 📊 데이터 관리에 **목록 표시 범위**(재수집 없음) — 글 **`date` 구간**(선택) + **표 상단부터 행 구간**(끝=0 이면 끝까지)으로 `target_df`만 좁히고 **전체 스냅샷 유지**.

## 1.3.24 — 2026-05-19

- **자동댓글러 DB:** 2단계 **타겟 재수집** 시 기존 `DELETE` 후 전량 INSERT로 **`comment_*` 결과가 초기화**되던 동작 수정. 같은 URL은 수집 직후에도 **`comment_status`/`comment_detail`/`comment_tried_at` 유지**(강제 종료 없이 결과가 줄어든 것처럼 보이는 주원인 완화).

## 1.3.23 — 2026-05-18

- **자동댓글러:** 📖 가이드에 **안전 사용 조건**(300건/일·당일 재실행 금지·제재 가능성) 정리. 댓글마다 **`{인사}` 무작위**(`COMMENTER_GREETING_VARIANTS`), 기본 템플릿·맨 앞 「안녕하세요」 치환.

## 1.3.22 — 2026-05-18

- **자동댓글러:** 📖 사용 가이드에 **댓글 간격·N건 단위 휴식** 설명 및 **하루 약 300건 이하 권장** 문구 추가(고정 정책, 커스텀 UI 없음).

## 1.3.21 — 2026-05-15

- **자동댓글러 템플릿:** `comment_templates.json`을 **`get_comment_templates_path()`**(exe·루트)로 고정, **`build.bat`**·**`pick_prior_dist_dir.ps1`** 마이그레이션에 포함. 기본 하드코딩 문장 **1개** + `(직접 입력)` 만 표시.
- **배포 ZIP:** **`pack_dist.ps1`** 에서 `comment_templates.json` 제외(로컬 설정 성격).
- **빌드:** exe 옆으로 **`version.txt` 복사 제거** — 폴더명 semver는 루트 `version.txt`, 앱 표시는 번들 `_internal` 유지.

## 1.3.20 — 2026-05-15

- **자동댓글러:** 목록/표 작성자 셀에 붙는 **`멤버등급 : 일반멤버`** 등 접미 제거 — `sanitize_commenter_nickname`, `_normalize_nickname` 보강, 템플릿 치환·댓글 작성·타겟 표(표시)에 적용.
- **댓글 미리보기:** 이메일 스타일 **`To. … :`** 제거, 치환된 **본문만** 표시.
- **기본 예시 템플릿:** `{닉네임}` → `{작성자}` (표·안내와 통일).

## 1.3.19 — 2026-05-15

- **자동댓글러:** 댓글 템플릿 **`{작성자}`** 토큰 추가(DB 컬럼 `nickname`·UI 「작성자」와 동일 값). **`{닉네임}`·`{제목}`** 유지. 치환 **`apply_comment_template_placeholders`** 로 통일(`bot.py`).
- **UI:** 템플릿 `help(?)` 제거, **`{작성자} 치환 가능`** 인라인 표기. 타겟 표 인덱스는 사용자 요청에 따라 숨기지 않음.

## 1.3.18 — 2026-05-15

- **자동댓글러 2단계:** 닉네임이 모두 `unknown`일 때 **`drop_duplicates(nickname)`으로 수백 건이 1건으로 줄어들던** 문제 수정 — `unknown` 행은 **URL 기준**으로 구분해 중복 제거.
- **목록 작성자 추출:** 표형 게시판(`td.td_name` 등) 셀렉터 보강, 유효 닉을 찾으면 즉시 채택(기존: `member_id`만으로 break); **`tr`/`td` 열 휴리스틱** 폴백. `_normalize_nickname`에서 목록 등급 **`[1]`** 접미 제거.

## 1.3.17 — 2026-05-15

- **자동댓글러 목록 수집:** `_switch_to_cafe_iframe()`이 `cafe_main` 대기 실패 시에도 **성공으로 넘어가 부모 문서에서 빈 목록을 보던** 케이스를 줄이기 위해 **mainFrame·name·src 기반 폴백**을 추가하고, 전부 실패 시 **안내 메시지·`False` 반환**.
- **목록 파싱:** `time[datetime]` 날짜, 제목 링크 셀렉터 보강; 목록 단계에서 **닉 레이어 클릭(`prefer_layer`) 비활성화**로 지연·간섭 완화.
- **타겟 DB 저장:** `replace_commenter_targets` 예외를 삼키지 않고 **`st.warning`** 으로 표시.

## 1.3.16 — 2026-05-14

- **자동댓글러 2단계:** 목록 수집 후 후보 글마다 `scrape_article_detail`을 호출하던 로직 제거 — 목록에 닉·게시판이 없으면 잠시 불완전하더라도 **수집이 사실상 멈춘 것처럼 보이던 병목** 해소. 목록 배치마다 진행 문구에 누적 건수 표시.

## 1.3.15 — 2026-05-14

- **`scrape_board_list` 견고화:** 네이버 카페 목록 DOM 변경 시 행만 잡히고 글이 0건 되던 경우를 줄이기 위해 **게시글 링크 기반 폴백 수집**(`_fallback_collect_board_list_rows`), 목록 행 셀렉터 확장, 행이 비어 보일 때도 폴백 시도.

## 1.3.14 — 2026-05-14

- **데이터 초기화 UI 통일:** 이벤트 댓글·카페 수집 메인(`app`)·자동댓글러 — 노란/경고 블록+이중 안내 없이 **`[ ] 안내 한 줄`** + **`데이터 초기화`** 버튼만 같은 형태로 정리.

## 1.3.13 — 2026-05-14

- **자동 댓글러 DB 패널:** 실제 적용 경로만 읽기 전용 표시·**DB 경로 저장** 제거·노란 안내 상자 제거. 초기화는 체크박스(안내 두 줄)·**댓글 대상 DB 초기화** 단추만 사용.

## 1.3.12 — 2026-05-14

- **`build.bat` semver 올릴 때 사용자 데이터 이관:** `dist\cafescraper_V{신버전}` 이 아직 없거나 비어 있어도, `dist` 안 **가장 semver가 큰 이전 `cafescraper_V*.*.*` 폴더**에서 `data\`, `crawler_config.json`, `sessions\`, `snapshots\`를 찾아 새 빌드 폴더로 백업·복구. (`scripts\pick_prior_dist_dir.ps1`)

## 1.3.11 — 2026-05-14

- **자동 댓글러:** `2단계: 타겟 목록 수집` 버튼이 먹통처럼 보이던 경우 수정 — `st.rerun()` 직후 `_commenter_run_collect` 플래그만 유실되면 `commenter_collecting=True` 만 남아 버튼이 계속 비활성처럼 동작할 수 있어, 클릭 시 **바로 `_collect_commenter_targets_into_session()` 호출**로 변경. 차단 상태에서는 Primary가 아니라 Secondary 버튼으로 표시하고 도움말 추가.

## 1.3.10 — 2026-05-14

- **자동 댓글러:** 카페명·카페 URL을 `crawler_config.json`에 저장한 뒤에도 페이지를 벗어나면 빈 칸처럼 보이던 현상 수정 — 새 세션에서 `commenter_cafe_*` 설정을 세션 상태에 채움, 저장되어 있으면 연결 단추는 `리셋` 상태로 표시.
- **자동 댓글러 UI:** DB 경로·댓글 대상 건수·초기화 블록을 **가운데 패널(타겟 수집 설정)** 하단으로 옮기고 **`💾 DB 경로 · 댓글 대상 (접기/펼치기)`** expander로 정리.

## 1.3.9 — 2026-05-14

- **`build.bat` 재빌드 시 사용자 데이터 이관 보강:** `data\` 외에 exe 옆 **`crawler_config.json`**, **`sessions\`**, **`snapshots\`** 도 백업·복구(기존엔 설정·세션만 초기화되는 문제 가능).
- semver 안내 문구 및 복구 실패 시 `dist\_user_data_backup` 유지 경고 보강.

## 1.3.8 — 2026-05-14

- **배포 폴더명에 버전 표기:** PyInstaller 산출물이 `dist\CafeScraper` 고정이 아니라 **`dist\cafescraper_V{semver}`** (`version.txt` 한 줄과 동일)로 생성되도록 `cafescraper.spec`·`build.bat`·`scripts\pack_dist.ps1` 정렬. 탐색기에서 빌드 구분 가능.
- 예전 레이아웃 `dist\CafeScraper\` 폴더가 남아 있으면 `build.bat` 끝에서 안내하고, 필요 시 직접 삭제하면 됩니다.

## 1.3.7 — 2026-05-13

- **카페명·URL 행 통일:** 카페 수집(`app.py`), 이벤트 댓글(`pages/03_event_comment_lottery.py`), 자동 댓글러(`pages/04_auto_commenter.py`) 모두 URL 줄을 `[입력창 | 저장 | 초기화]` 3열로 맞춤. 리셋 후에만 저장이 보이던 이중 단계 제거.
- 메인 카페 수집: 카페명 입력 기본값에서 `'카페 몬스터'` 자동 채움 제거(저장된 설정만 표시, 없으면 빈 칸).

## 1.3.6 — 2026-05-13

- 번들에 `pyperclip`이 빠져 자동 댓글러에서 `ModuleNotFoundError`가 나던 문제: 빌드 환경에 패키지 설치 및 `cafescraper.spec`에서 `collect_all('pyperclip')`.
- 데스크톱 빌드에서 `webview` 미포함 방지를 위해 `run_app.py` 모듈 레벨 정적 import 유지.
- **버전 표기:** `version.txt` 단일 소스, `app/utils/app_version.py`, Streamlit 탭 제목·pywebview 창 제목 반영, 번들 `_internal` 및 배포 폴더에 `version.txt` 포함·복사, 본 변경 이력 파일 추가.

## 1.3.5 및 이전

- 세부 내용은 Git 기록 및 `docs/help.md` 등 문서를 참고합니다.
